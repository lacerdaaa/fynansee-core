export enum ImportBatchStatus {
  Uploaded = 'uploaded',
  Processed = 'processed',
  Failed = 'failed',
}

export enum ImportRowStatus {
  Pending = 'pending',
  Error = 'error',
  Applied = 'applied',
}
